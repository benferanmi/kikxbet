
import ProfileLayout from '../ProfileLayout';

const page = () => {
    return (
        <ProfileLayout>
            <div>Verification</div>
        </ProfileLayout>
    );
};

export default page;