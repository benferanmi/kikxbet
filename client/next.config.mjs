/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.flashscore.com', "cdn.opticodds.com", "cdn.pandascore.co", "a.espncdn.com", "cdn.oddsjam.com"],
      },
};

export default nextConfig;
